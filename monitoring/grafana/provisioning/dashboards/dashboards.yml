apiVersion: 1

providers:
  - name: 'Auth Dashboards'
    orgId: 1
    folder: 'Auth Service'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 15
    options:
      path: /var/lib/grafana/dashboards
